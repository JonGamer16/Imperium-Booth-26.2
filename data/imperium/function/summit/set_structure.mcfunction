# Battlegrounds structure override — intentionally blank until the custom arena structure
# ships (register_booth requires a valid function here even if empty). When the structure
# file exists, call:
# function summit.battlegrounds:api/set_structure {booth_id:"imperium", structure:"imperium:arena"}
